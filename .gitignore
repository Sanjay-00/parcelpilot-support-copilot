ass1/
AGENTS.md
docs/STUDY.md
__pycache__/
*.pyc
.env
parcelpilot.db
*.stackdump
.pytest_cache/
AI Agent Assessment - Candidate Pack/*
# The workbook is the only file the app reads at runtime (see README "Data pack")
# and is committed so a hosted deployment built straight from this repo is
# self-contained; the 6 policy/agreement PDFs are excluded since nothing in the
# app parses them at runtime.
!AI Agent Assessment - Candidate Pack/ParcelPilot_Assessment_Data.xlsx
